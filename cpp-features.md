# 项目 C++ 特性清单

范围：
- `include/BS_thread_pool.hpp`
- `modules/BS.thread_pool.cppm`
- `tests/BS_thread_pool_test.cpp`

行号是该特性的代表位置，同一特性多处出现时不逐行列出。Ctrl+点击链接打开文件并定位到该行；区间链接定位到起始行。第四列按这些位置说明该语法的概念、在本仓库中的用法和注意点。

## 预处理与模块

| 特性术语 | 英文术语 | 所在文件行号 | 语法说明 |
| --- | --- | --- | --- |
| 头文件保护宏 | include guard | [include/BS_thread_pool.hpp:17-18](include/BS_thread_pool.hpp#L17) [include/BS_thread_pool.hpp:2510](include/BS_thread_pool.hpp#L2510) | `#ifndef` / `#define` / `#endif` 使头文件在一次翻译单元里只展开一次。17–18 行定义 `BS_THREAD_POOL_HPP`，2510 行结束。宏名须全工程唯一；模块接口单元不再靠它防重入。 |
| 函数式宏 | function-like macro | [include/BS_thread_pool.hpp:273-281](include/BS_thread_pool.hpp#L273) [include/BS_thread_pool.hpp:373-375](include/BS_thread_pool.hpp#L373) [include/BS_thread_pool.hpp:444](include/BS_thread_pool.hpp#L444) | 带形参的宏在预处理阶段做文本替换。273 行按运算符生成位运算函数，373 行按是否有 `jthread` 生成不同的形参文本，444 行在 concepts 与 SFINAE 之间切换模板头。实参不会求值，多次出现会多次展开；能用模板、`if constexpr` 或函数就不要用宏。 |
| 记号粘贴 `##` | token pasting | [include/BS_thread_pool.hpp:278](include/BS_thread_pool.hpp#L278) | `##` 把左右记号拼成一个新记号。278 行把 `OP` 与 `=` 拼成复合赋值运算符。拼接结果必须是合法记号；宏参数若本身是宏，`##` 不会先展开它。 |
| 条件编译 | conditional compilation | [include/BS_thread_pool.hpp:21-113](include/BS_thread_pool.hpp#L21)；[tests/BS_thread_pool_test.cpp:25-85](tests/BS_thread_pool_test.cpp#L25) | `#if` / `#ifdef` / `#elif` / `#else` 按宏决定哪些源码进入编译。头文件按平台、`__cplusplus` 和特性宏选择头文件或 `import std`；测试在 25–85 行同样选择包含头还是导入模块。未选中的分支不参与语法检查，但括号和 `#endif` 必须配对。 |
| `#error` | `#error` directive | [include/BS_thread_pool.hpp:71](include/BS_thread_pool.hpp#L71) | 预处理期直接让编译失败并打印消息。71 行在头文件模式下却定义了 `BS_THREAD_POOL_IMPORT_STD` 时中止，因为那样会强迫包含者使用 `import std`。只用于无法继续的配置错误，不要代替运行期检查。 |
| `#include` / `#undef` | source inclusion / macro undefinition | [include/BS_thread_pool.hpp:23](include/BS_thread_pool.hpp#L23) [include/BS_thread_pool.hpp:30](include/BS_thread_pool.hpp#L30) [include/BS_thread_pool.hpp:74](include/BS_thread_pool.hpp#L74) [include/BS_thread_pool.hpp:117](include/BS_thread_pool.hpp#L117) | `#include` 把另一文件插入当前点；`#undef` 取消宏定义。23 行包含 `<version>`，74 行在不满足条件时取消 `BS_THREAD_POOL_IMPORT_STD`，117 行取消 Linux 的 `major`。`#undef` 只影响本翻译单元之后的代码；取消标准宏或系统宏要确认后续不再依赖它。 |
| `__has_include` | `__has_include` | [include/BS_thread_pool.hpp:21-22](include/BS_thread_pool.hpp#L21)；[tests/BS_thread_pool_test.cpp:18-19](tests/BS_thread_pool_test.cpp#L18) [tests/BS_thread_pool_test.cpp:767](tests/BS_thread_pool_test.cpp#L767) | 预处理运算符，询问某个头文件是否能被找到。21–22 行先判断编译器是否支持该运算符，再探测 `<version>`。它只表示“找得到文件”，不表示其中的声明可用；外层还要用 `#ifdef __has_include` 兼容老编译器。 |
| 特性测试宏 | feature-test macro | [include/BS_thread_pool.hpp:29](include/BS_thread_pool.hpp#L29) [include/BS_thread_pool.hpp:97](include/BS_thread_pool.hpp#L97) [include/BS_thread_pool.hpp:100](include/BS_thread_pool.hpp#L100) [include/BS_thread_pool.hpp:104](include/BS_thread_pool.hpp#L104) [include/BS_thread_pool.hpp:107](include/BS_thread_pool.hpp#L107) [include/BS_thread_pool.hpp:110](include/BS_thread_pool.hpp#L110) [include/BS_thread_pool.hpp:148](include/BS_thread_pool.hpp#L148) [include/BS_thread_pool.hpp:296](include/BS_thread_pool.hpp#L296) [include/BS_thread_pool.hpp:367](include/BS_thread_pool.hpp#L367)；[tests/BS_thread_pool_test.cpp:67-70](tests/BS_thread_pool_test.cpp#L67) [tests/BS_thread_pool_test.cpp:697-762](tests/BS_thread_pool_test.cpp#L697) | 标准或实现用 `__cpp_*` 表示某语言或库特性是否可用。这里用 `__cpp_lib_jthread`、`__cpp_concepts`、`__cpp_impl_three_way_comparison` 等决定包含哪些头、走哪条实现。宏可能被前面的 `#undef` 关掉（30 行），不能只看标准版本号。 |
| `__cplusplus` | predefined macro `__cplusplus` | [include/BS_thread_pool.hpp:28](include/BS_thread_pool.hpp#L28) [include/BS_thread_pool.hpp:66](include/BS_thread_pool.hpp#L66)；[tests/BS_thread_pool_test.cpp:25](tests/BS_thread_pool_test.cpp#L25) [tests/BS_thread_pool_test.cpp:78](tests/BS_thread_pool_test.cpp#L78) | 编译器按语言模式定义的长整型常量：C++17 为 `201703L`，C++20 为 `202002L`。66 行要求至少 `202004L` 才允许 `import std`，78 行要求 `202002L` 才 `import` 具名模块。MSVC 需 `/Zc:__cplusplus` 才会给出正确值，否则长期停在 `199711L`。 |
| 编译器 / 平台预定义宏 | predefined compiler and platform macros | [include/BS_thread_pool.hpp:28](include/BS_thread_pool.hpp#L28) [include/BS_thread_pool.hpp:35](include/BS_thread_pool.hpp#L35) [include/BS_thread_pool.hpp:43](include/BS_thread_pool.hpp#L43) [include/BS_thread_pool.hpp:51](include/BS_thread_pool.hpp#L51) | 实现预先定义、用来识别编译器和系统。28 行用 `__clang__` 与 `_LIBCPP_VERSION`，35 行用 `__GNUC__`、`_GLIBCXX_RELEASE`、`_WIN32`，43–51 行区分 Windows 与 Linux/Apple。同一编译器可能同时定义多个宏；平台分支未命中时 61 行会 `#undef` 掉原生扩展。 |
| 全局模块片段 | global module fragment | [modules/BS.thread_pool.cppm:17-22](modules/BS.thread_pool.cppm#L17) | `module;` 到 `export module` 之间的区域属于全局模块片段，只能放预处理指令和 `#include`。这里定义 `BS_THREAD_POOL_MODULE` 并包含头文件，使头里的声明进入模块。片段里不要写普通声明；被包含的头不应再被使用者 `#include`，否则容易 ODR 冲突。 |
| 模块接口单元 `export module` | module interface unit | [modules/BS.thread_pool.cppm:24](modules/BS.thread_pool.cppm#L24) | `export module 名字;` 声明本文件是该模块的接口。名字是 `BS.thread_pool`，点号只是名字的一部分，不是嵌套。一个模块只能有一个接口单元；实现单元用 `module 名字;` 且不能导出新接口。 |
| `import` 具名模块 | named module import | [tests/BS_thread_pool_test.cpp:79](tests/BS_thread_pool_test.cpp#L79) | `import 模块名;` 使已导出的声明在当前翻译单元可见，且只在该宏和 C++20 同时满足时编译。导入不是文本包含，宏不会从模块里传出来。必须先编译出模块接口，且不要再包含同一份头。 |
| `export namespace` 与 `using` 再导出 | exported namespace and using-declaration | [modules/BS.thread_pool.cppm:26-47](modules/BS.thread_pool.cppm#L26) | `export namespace` 导出整个命名空间；内部的 `using` 把已有名字重新导出给导入者。26–47 行把 `thread_pool`、运算符和 `version` 等从 `BS` 再导出。`using` 不会导出宏；未列在这里的名字对导入者不可见。 |
| `import std`（C++23 标准库模块） | standard library module `import std` | [include/BS_thread_pool.hpp:69](include/BS_thread_pool.hpp#L69)；[tests/BS_thread_pool_test.cpp:26](tests/BS_thread_pool_test.cpp#L26) | C++23 用 `import std;` 一次性导入标准库，代替大量 `#include`。头文件只在作为模块编译且宏开启时才导入；测试在 26 行同样分支。`import std` 与随后 `#include` 标准头混用容易出错，本库因此把系统头放在导入之前，并在不支持时退回头文件。 |

## 类型、对象与初始化

| 特性术语 | 英文术语 | 所在文件行号 | 语法说明 |
| --- | --- | --- | --- |
| 命名空间 | namespace | [include/BS_thread_pool.hpp:134](include/BS_thread_pool.hpp#L134) | `namespace 名字 { }` 把声明放进独立作用域，避免和全局名字冲突。134 行起全部库接口都在 `BS` 中。命名空间可以多次打开并合并；不要在头文件用 `using namespace`。 |
| 匿名命名空间 | unnamed namespace | [tests/BS_thread_pool_test.cpp:92](tests/BS_thread_pool_test.cpp#L92) | `namespace { }` 里的名字具有内部链接，只在本翻译单元可见。测试从 92 行起把 `sync_cout` 等辅助对象放在这里。每个 `.cpp` 的匿名命名空间互相独立；不要放进头文件，否则每个包含者都会有一份。 |
| 类型别名 `using` | type alias | [include/BS_thread_pool.hpp:244](include/BS_thread_pool.hpp#L244) [include/BS_thread_pool.hpp:365](include/BS_thread_pool.hpp#L365) [include/BS_thread_pool.hpp:371](include/BS_thread_pool.hpp#L371)；[tests/BS_thread_pool_test.cpp:259](tests/BS_thread_pool_test.cpp#L259) | `using 新名 = 类型;` 给已有类型起别名，不产生新类型。244 行 `opt_t` 是 `uint8_t`，365 行 `task_t` 是任务包装，371 行 `thread_t` 在 `jthread` 与 `thread` 之间切换。别名透明，重载和模板仍按原类型匹配。 |
| `auto` 类型推导 | `auto` type deduction | [tests/BS_thread_pool_test.cpp:891](tests/BS_thread_pool_test.cpp#L891) [tests/BS_thread_pool_test.cpp:1007](tests/BS_thread_pool_test.cpp#L1007) [tests/BS_thread_pool_test.cpp:1369](tests/BS_thread_pool_test.cpp#L1369) | `auto` 按初始化表达式推导类型，规则接近模板实参推导。测试里用它接住 lambda、future 或成员指针，避免写出很长的类型。`auto` 会丢掉顶层引用和 cv；需要引用时要写 `auto&` 或 `const auto&`。 |
| `nullptr` | `nullptr` | [include/BS_thread_pool.hpp:358](include/BS_thread_pool.hpp#L358) [include/BS_thread_pool.hpp:1084](include/BS_thread_pool.hpp#L1084) [include/BS_thread_pool.hpp:2386](include/BS_thread_pool.hpp#L2386) | `nullptr` 是 `std::nullptr_t` 的空指针常量，可转成任意指针类型。358 行把类型擦除指针默认成空，2386 行把线程数组的 `unique_ptr` 默认成空。不要用整数 `0` 表示空指针，否则重载时可能匹配到整数版本。 |
| 定宽整数 | fixed-width integer types | [include/BS_thread_pool.hpp:145](include/BS_thread_pool.hpp#L145) [include/BS_thread_pool.hpp:244](include/BS_thread_pool.hpp#L244) [include/BS_thread_pool.hpp:392](include/BS_thread_pool.hpp#L392)；[tests/BS_thread_pool_test.cpp:269](tests/BS_thread_pool_test.cpp#L269) | `<cstdint>` 的 `std::uintN_t` / `std::intN_t` 宽度固定。版本号用 `uint64_t`，位掩码用 `uint8_t`，优先级用正好 8 位的 `int8_t`（-128 到 127）。这些类型在某平台可能不存在；需要“至少 N 位”时用 `uint_leastN_t`。 |
| std::size_t / std::ptrdiff_t | std::size_t / std::ptrdiff_t | [include/BS_thread_pool.hpp:493](include/BS_thread_pool.hpp#L493) [include/BS_thread_pool.hpp:1385](include/BS_thread_pool.hpp#L1385)；[tests/BS_thread_pool_test.cpp:121](tests/BS_thread_pool_test.cpp#L121) [tests/BS_thread_pool_test.cpp:146](tests/BS_thread_pool_test.cpp#L146) | `size_t` 是无符号的大小类型，`ptrdiff_t` 是有符号的指针差值类型。493 行用 `size_t` 数就绪的 future，测试里信号量上限用 `ptrdiff_t`。无符号减法下溢会变成很大的数；和有符号数比较前要统一类型。 |
| 作用域枚举，指定底层类型 | scoped enumeration `enum class` with fixed underlying type | [include/BS_thread_pool.hpp:249](include/BS_thread_pool.hpp#L249)；[tests/BS_thread_pool_test.cpp:269](tests/BS_thread_pool_test.cpp#L269) | `enum class 名 : 底层类型` 的枚举项不泄漏到外层，也不能隐式变成整数。`tp` 的底层类型是 `uint8_t`，标志用 `1 << n`。要做位运算必须先转成底层类型或重载运算符，本库在 273 行用宏生成这些运算符。 |
| 非作用域枚举，指定底层类型 | unscoped enumeration with fixed underlying type | [include/BS_thread_pool.hpp:397](include/BS_thread_pool.hpp#L397) | `enum 名 : 类型` 的枚举项进入外围作用域，并可隐式转为整数。`pr` 故意不用 `enum class`，这样 `lowest` 等可以直接当 `int8_t` 优先级用。枚举项可能和别的名字冲突；新代码更常选用作用域枚举。 |
| 成员初始化列表 | member initializer list | [include/BS_thread_pool.hpp:145](include/BS_thread_pool.hpp#L145) [include/BS_thread_pool.hpp:323](include/BS_thread_pool.hpp#L323) [include/BS_thread_pool.hpp:417](include/BS_thread_pool.hpp#L417)；[tests/BS_thread_pool_test.cpp:132](tests/BS_thread_pool_test.cpp#L132) | 构造函数冒号后的列表按成员声明顺序初始化成员，而不是按列表书写顺序。145 行初始化版本号三字段，323 行用转发后的可调用对象构造 `unique_ptr`。引用、const 成员和没有默认构造的成员必须在这里初始化。 |
| 类内默认成员初始化 | default member initializer | [include/BS_thread_pool.hpp:358](include/BS_thread_pool.hpp#L358) [include/BS_thread_pool.hpp:439](include/BS_thread_pool.hpp#L439) [include/BS_thread_pool.hpp:640](include/BS_thread_pool.hpp#L640) [include/BS_thread_pool.hpp:2361](include/BS_thread_pool.hpp#L2361) [include/BS_thread_pool.hpp:2391](include/BS_thread_pool.hpp#L2391) | 成员声明处的 `= 值` 在构造函数没有显式初始化该成员时生效。指针默认 `nullptr`，优先级默认 `0`，清理函数默认空 lambda，`paused` 用 `{}`。初始化列表里的显式初始化会覆盖默认值。 |
| 委托构造函数 | delegating constructor | [include/BS_thread_pool.hpp:1457](include/BS_thread_pool.hpp#L1457) [include/BS_thread_pool.hpp:1464](include/BS_thread_pool.hpp#L1464) [include/BS_thread_pool.hpp:1472](include/BS_thread_pool.hpp#L1472) | 构造函数的初始化列表只调用同类的另一个构造函数，把真正的初始化集中在一处。1457 行 `thread_pool()` 委托给接收线程数和初始化函数的构造函数。委托构造不能再初始化其他成员；被委托者返回后才能执行本构造函数体。 |
| 聚合初始化 | aggregate initialization | [include/BS_thread_pool.hpp:2169](include/BS_thread_pool.hpp#L2169) [include/BS_thread_pool.hpp:2204](include/BS_thread_pool.hpp#L2204) [include/BS_thread_pool.hpp:2237](include/BS_thread_pool.hpp#L2237)；[tests/BS_thread_pool_test.cpp:3304](tests/BS_thread_pool_test.cpp#L3304) [tests/BS_thread_pool_test.cpp:3348](tests/BS_thread_pool_test.cpp#L3348) | 聚合没有用户声明的构造函数、没有 private/protected 非静态成员，可以用花括号按成员顺序初始化。这些位置用花括号构造块任务描述或测试数据。一旦为该类声明了构造函数，它就不再是聚合。 |
| 列表初始化 | list initialization | [tests/BS_thread_pool_test.cpp:317-324](tests/BS_thread_pool_test.cpp#L317) | 用 `{}` 初始化对象，优先考虑 `initializer_list` 构造函数，并禁止变窄转换。317 行起用列表构造测试用的参数序列。`{}` 对内置类型是零初始化；有 `initializer_list` 构造时，`{1,2}` 不会去调两个参数的普通构造。 |
| 值初始化 `{}` | value initialization | [include/BS_thread_pool.hpp:1104](include/BS_thread_pool.hpp#L1104) [include/BS_thread_pool.hpp:1154](include/BS_thread_pool.hpp#L1154) [include/BS_thread_pool.hpp:2391](include/BS_thread_pool.hpp#L2391) | 空花括号对类调用默认构造，对标量零初始化。1104、1154 行得到空的 optional 或默认结果，2391 行把 `bool` 或 `monostate` 做成默认状态。不要和“默认初始化”（不写初始化器、标量值不确定）混淆。 |
| 默认实参 | default arguments | [include/BS_thread_pool.hpp:417](include/BS_thread_pool.hpp#L417) [include/BS_thread_pool.hpp:1427](include/BS_thread_pool.hpp#L1427) [include/BS_thread_pool.hpp:1531](include/BS_thread_pool.hpp#L1531)；[tests/BS_thread_pool_test.cpp:170](tests/BS_thread_pool_test.cpp#L170) [tests/BS_thread_pool_test.cpp:427](tests/BS_thread_pool_test.cpp#L427) | 形参可以自右向左给出默认值，调用时省略则使用默认值。417 行优先级默认为 0，1427 行模板参数 `OptFlags` 默认为 `tp::none`。默认实参在声明处求值上下文确定，函数重载时每个参数只能在一处声明默认值。 |
| 函数局部 `static` | function-local static | [tests/BS_thread_pool_test.cpp:557-558](tests/BS_thread_pool_test.cpp#L557) | 函数内的 `static` 局部变量只初始化一次，之后调用复用同一对象。测试用它保存随机数引擎，避免每次重建。C++11 起初始化是线程安全的；析构发生在程序退出时，不要依赖析构顺序。 |
| `static_cast` | `static_cast` | [include/BS_thread_pool.hpp:276](include/BS_thread_pool.hpp#L276) [include/BS_thread_pool.hpp:290](include/BS_thread_pool.hpp#L290) [include/BS_thread_pool.hpp:588](include/BS_thread_pool.hpp#L588) [include/BS_thread_pool.hpp:920](include/BS_thread_pool.hpp#L920)；[tests/BS_thread_pool_test.cpp:337](tests/BS_thread_pool_test.cpp#L337) | 编译期可见的、有定义的转换，如数值、枚举和继承指针。276、290 行在枚举与其底层整数之间来回转，才能做位运算再变回 `tp`。它不做运行期检查；无关指针之间的转换要用 `reinterpret_cast`，那是另一类更危险的转换。 |
| 函数指针类型转换 | function pointer cast | [include/BS_thread_pool.hpp:2491](include/BS_thread_pool.hpp#L2491) [include/BS_thread_pool.hpp:2496](include/BS_thread_pool.hpp#L2496) | 把函数指针显式转成另一种函数指针类型。这里把 `std::endl` / `std::flush` 转成 `ostream& (&)(ostream&)`，以便当作流操纵符保存。函数指针类型必须完全匹配调用约定和签名；转错后调用是未定义行为。 |
| 整数后缀 `U` / `ULL` | integer literal suffixes | [include/BS_thread_pool.hpp:856](include/BS_thread_pool.hpp#L856) [include/BS_thread_pool.hpp:862](include/BS_thread_pool.hpp#L862) | 后缀决定字面量类型：`U` 是 `unsigned`，`ULL` 是 `unsigned long long`。856 行 `1U` 避免有符号右移，862 行 `1ULL << i` 保证移位宽度够 64 位。移位次数必须小于该类型的位数，否则行为未定义。[见：function_pointer_cast.md](function_pointer_cast.md)|
| 十六进制转义字符串 | hexadecimal escape in string literal | [tests/BS_thread_pool_test.cpp:3488](tests/BS_thread_pool_test.cpp#L3488) | `\xHH` 在字符串里插入指定字节。测试用它构造含非文本字节的数据。`\x` 会尽量吞掉后续十六进制数字，后面若紧挨着合法十六进制字符会被吃进转义；可用 `\xHH""` 断开。 |

## 函数、运算符与属性

| 特性术语 | 英文术语 | 所在文件行号 | 语法说明 |
| --- | --- | --- | --- |
| `constexpr` 构造函数 / 函数 / 变量 | `constexpr` constructor, function, and variable | [include/BS_thread_pool.hpp:145](include/BS_thread_pool.hpp#L145) [include/BS_thread_pool.hpp:151](include/BS_thread_pool.hpp#L151) [include/BS_thread_pool.hpp:201](include/BS_thread_pool.hpp#L201) [include/BS_thread_pool.hpp:1434](include/BS_thread_pool.hpp#L1434)；[tests/BS_thread_pool_test.cpp:132](tests/BS_thread_pool_test.cpp#L132) [tests/BS_thread_pool_test.cpp:146](tests/BS_thread_pool_test.cpp#L146) [tests/BS_thread_pool_test.cpp:317](tests/BS_thread_pool_test.cpp#L317) | `constexpr` 表示该函数或变量可以在编译期求值，实参也是常量表达式时结果就是常量。145 行版本号构造、151 行比较、201 行 `thread_pool_version`、1434 行把模板参数算成 `priority_enabled`。函数体只能做常量表达式允许的操作；运行期调用仍然合法。 |
| 内联变量 `inline` | inline variable | [include/BS_thread_pool.hpp:201](include/BS_thread_pool.hpp#L201) [include/BS_thread_pool.hpp:209](include/BS_thread_pool.hpp#L209) [include/BS_thread_pool.hpp:1347](include/BS_thread_pool.hpp#L1347)；[include/BS_thread_pool.hpp:2491](include/BS_thread_pool.hpp#L2491) | C++17 起 `inline` 变量可以在头文件里定义，多个翻译单元合并为同一实体。201、209 行的版本和模块标志因此能放在头里。非 `inline` 的非 const 全局变量放头文件会违反 ODR。 |
| `static_assert` | `static_assert` | [include/BS_thread_pool.hpp:205](include/BS_thread_pool.hpp#L205) [include/BS_thread_pool.hpp:1447](include/BS_thread_pool.hpp#L1447)；[tests/BS_thread_pool_test.cpp:80-89](tests/BS_thread_pool_test.cpp#L80) [tests/BS_thread_pool_test.cpp:124](tests/BS_thread_pool_test.cpp#L124) | 编译期断言，条件为假就编译失败，并可带说明字符串。205 行核对模块与头的版本，1447 行禁止在关闭异常时启用死锁检查，测试核对导入方式是否和宏一致。条件必须是常量表达式；它不能检查运行期状态。 |
| `noexcept` 说明符 | `noexcept` specifier | [include/BS_thread_pool.hpp:145](include/BS_thread_pool.hpp#L145) [include/BS_thread_pool.hpp:288](include/BS_thread_pool.hpp#L288) [include/BS_thread_pool.hpp:316](include/BS_thread_pool.hpp#L316) [include/BS_thread_pool.hpp:1495](include/BS_thread_pool.hpp#L1495) | `noexcept` 保证函数不抛异常。版本构造、位运算、移动构造和析构都标了它，便于容器和 `std::move_if_noexcept` 选择移动。若 `noexcept` 函数真的抛出，程序直接调用 `terminate`，不会被外层 `catch` 接住。 |
| 条件 `noexcept` | conditional `noexcept` | [include/BS_thread_pool.hpp:417](include/BS_thread_pool.hpp#L417) [include/BS_thread_pool.hpp:2125](include/BS_thread_pool.hpp#L2125) | `noexcept(布尔常量表达式)` 按条件决定是否不抛。417 行当 `task_t` 的移动构造不抛时，`pr_task` 构造也不抛。条件本身必须在声明时就能算出来；它不捕获异常，只影响类型系统。 |
| 显式构造函数 `explicit` | `explicit` constructor | [include/BS_thread_pool.hpp:341](include/BS_thread_pool.hpp#L341) [include/BS_thread_pool.hpp:417](include/BS_thread_pool.hpp#L417) [include/BS_thread_pool.hpp:1464](include/BS_thread_pool.hpp#L1464)；[tests/BS_thread_pool_test.cpp:132](tests/BS_thread_pool_test.cpp#L132) | `explicit` 禁止用该构造函数做隐式转换。单实参的 `pr_task`、线程池构造和测试里的信号量构造都加上它，避免整数或可调用对象被悄悄转换成这些类型。需要转换时必须直接初始化，如 `T x(a)` 或 `T{a}`。 |
| 默认实参与重载的调用运算符 | function call operator `operator()` | [include/BS_thread_pool.hpp:325](include/BS_thread_pool.hpp#L325) [include/BS_thread_pool.hpp:673](include/BS_thread_pool.hpp#L673) [include/BS_thread_pool.hpp:692](include/BS_thread_pool.hpp#L692) [include/BS_thread_pool.hpp:713](include/BS_thread_pool.hpp#L713) | `operator()` 让对象能像函数一样调用。325 行类型擦除包装按保存的参数转发调用；后面几处是不同任务包装的调用运算符，用来执行已保存的工作。`operator()` 可以重载和带默认实参；const 对象只能调用 const 版本，C++23 的 `move_only_function::operator()` 不是 const 的。 |
| 比较运算符重载 | comparison operator overloading | [include/BS_thread_pool.hpp:151-178](include/BS_thread_pool.hpp#L151) [include/BS_thread_pool.hpp:426](include/BS_thread_pool.hpp#L426) | 为类定义 `==`、`<` 等，使对象能比较。没有三路比较时，151–178 行用 `tuple` 的比较生成版本号的六个关系；426 行按优先级比较任务。应保持一致：`a < b` 与 `b < a` 不能同时为真。C++20 通常改写一个 `<=>` 即可。 |
| 流插入运算符 | stream insertion operator `operator<<` | [include/BS_thread_pool.hpp:187](include/BS_thread_pool.hpp#L187)；[tests/BS_thread_pool_test.cpp:333](tests/BS_thread_pool_test.cpp#L333) [tests/BS_thread_pool_test.cpp:347](tests/BS_thread_pool_test.cpp#L347) | `operator<<(ostream&, T)` 定义如何把对象写入流。187 行把 `version` 打印成 `major.minor.patch` 并返回流本身，才能连续 `<<`。左操作数必须是流的非 const 引用；通常做成隐藏友元，以便找到类的私有成员。 |
| 位运算符重载（`& \| ^ ~` 及复合赋值） | bitwise and compound assignment operators | [include/BS_thread_pool.hpp:284-290](include/BS_thread_pool.hpp#L284) | 为枚举重载按位与、或、异或、取反及 `&=` 等，使作用域枚举能当位掩码用。284–286 行用宏展开出 `tp` 的二元运算，288 行单独写 `~`。实现里先转到 `uint8_t` 再转回来；复合赋值应返回左操作数的引用。 |
| 友元函数（隐藏友元） | hidden friend | [include/BS_thread_pool.hpp:151](include/BS_thread_pool.hpp#L151) [include/BS_thread_pool.hpp:187](include/BS_thread_pool.hpp#L187) [include/BS_thread_pool.hpp:426](include/BS_thread_pool.hpp#L426) | 定义在类内部的友元函数只能通过实参依赖查找（ADL）找到，不会污染命名空间。比较和 `operator<<` 都写成 `version` 或 `pr_task` 的友元。这种函数不能像普通成员那样用 `obj.f()` 调用，也不能靠 `BS::operator==` 这种限定名找到，除非再导出。 |
| 友元类 | friend class | [include/BS_thread_pool.hpp:968-969](include/BS_thread_pool.hpp#L968) | `friend class` 允许另一个类访问自己的私有成员。`this_thread` 把所有 `thread_pool<tp>` 特化声明为友元，这样线程池能改 `my_index`、`my_pool`。友元不传递、也不继承；前向声明的友元在它被定义之前不能访问。 |
| 三路比较，默认生成 | three-way comparison `operator<=>` defaulted | [include/BS_thread_pool.hpp:149](include/BS_thread_pool.hpp#L149) | C++20 的 `<=>` 一次给出小于、等于、大于。`= default` 按成员依次比较。149 行在有该特性时为 `version` 生成它，编译器同时合成 `==` 和各个关系运算符。成员类型自己也要能三路比较；没有该特性时本库退回手写六个运算符。 |
| `std::strong_ordering` | `std::strong_ordering` | [include/BS_thread_pool.hpp:149](include/BS_thread_pool.hpp#L149) | 三路比较的结果类型之一，表示全序且相等即可替换。整数版本号用它，而不是 `partial_ordering`（浮点可能无序）。返回类型要和比较的语义一致；默认的 `<=>` 会按成员的公共结果类型决定。 |
| 属性 `[[nodiscard]]` | attribute `[[nodiscard]]` | [include/BS_thread_pool.hpp:143](include/BS_thread_pool.hpp#L143) [include/BS_thread_pool.hpp:182](include/BS_thread_pool.hpp#L182) [include/BS_thread_pool.hpp:409](include/BS_thread_pool.hpp#L409) [include/BS_thread_pool.hpp:459](include/BS_thread_pool.hpp#L459) [include/BS_thread_pool.hpp:1428](include/BS_thread_pool.hpp#L1428) | 提示调用者不要丢弃返回值或临时对象。标在 `version`、`to_string`、`multi_future`、`thread_pool` 上，丢掉构造结果或返回的字符串会告警。它只是警告，不是错误；有意忽略时可以强制转换成 `void`。 |
| 属性 `[[maybe_unused]]` | attribute `[[maybe_unused]]` | [include/BS_thread_pool.hpp:887](include/BS_thread_pool.hpp#L887) [include/BS_thread_pool.hpp:1048](include/BS_thread_pool.hpp#L1048) | 告诉编译器这个名字可以不被读取，不要报未使用警告。用于条件编译下某些平台用不到的参数或变量。不要用它掩盖真正的逻辑错误；能删掉的无用参数就不要留。 |
| 基于范围的 `for` | range-based `for` | [include/BS_thread_pool.hpp:474](include/BS_thread_pool.hpp#L474) [include/BS_thread_pool.hpp:1556](include/BS_thread_pool.hpp#L1556) [include/BS_thread_pool.hpp:2462](include/BS_thread_pool.hpp#L2462)；[tests/BS_thread_pool_test.cpp:353](tests/BS_thread_pool_test.cpp#L353) | `for (声明 : 范围)` 依次遍历 `begin`/`end` 之间的元素。474 行遍历 future，1556 行遍历任务，2462 行遍历输出流。循环中不要使迭代器失效（例如边遍历边 `erase`）；需要下标时仍用传统 `for`。 |
| `switch` | `switch` statement | [include/BS_thread_pool.hpp:924](include/BS_thread_pool.hpp#L924) [include/BS_thread_pool.hpp:1178](include/BS_thread_pool.hpp#L1178) [include/BS_thread_pool.hpp:1249](include/BS_thread_pool.hpp#L1249) | 按整型或枚举跳到匹配的 `case`，没有 `break` 会落到下一支。这些位置按优先级或调度策略分支。每个 `case` 结束要 `break` 或 `return`；若故意贯穿，应写注释。条件不能是字符串或浮点。 |
| Lambda 表达式 | lambda expression | [include/BS_thread_pool.hpp:1457](include/BS_thread_pool.hpp#L1457) [include/BS_thread_pool.hpp:2303](include/BS_thread_pool.hpp#L2303) [include/BS_thread_pool.hpp:2361](include/BS_thread_pool.hpp#L2361)；[tests/BS_thread_pool_test.cpp:891](tests/BS_thread_pool_test.cpp#L891) [tests/BS_thread_pool_test.cpp:1375](tests/BS_thread_pool_test.cpp#L1375) | 就地写出的匿名函数对象。1457 行用空 lambda 当默认初始化函数，2303 行是条件变量的谓词，2361 行是默认清理函数。每个 lambda 有唯一类型；要存进一个变量就得类型擦除，或用 `auto`。无捕获的 lambda 才能转成函数指针。 |
| 按引用 / 按值捕获 | capture by reference and by copy | [include/BS_thread_pool.hpp:2089](include/BS_thread_pool.hpp#L2089)；[tests/BS_thread_pool_test.cpp:891](tests/BS_thread_pool_test.cpp#L891) [tests/BS_thread_pool_test.cpp:1466](tests/BS_thread_pool_test.cpp#L1466) | `[this, i]` 按值复制 `i`、按指针捕获对象；`[&]` 捕获引用，`[=]` 捕获副本。工作线程的 lambda 若按引用捕获栈上局部变量，函数返回后引用悬空。会离开当前作用域执行的任务应按值捕获，或捕获指针时保证对象仍然存活。 |
| 初始化捕获 | init-capture | [include/BS_thread_pool.hpp:735](include/BS_thread_pool.hpp#L735) [include/BS_thread_pool.hpp:2072](include/BS_thread_pool.hpp#L2072)；[tests/BS_thread_pool_test.cpp:1007](tests/BS_thread_pool_test.cpp#L1007) | `[名字 = 表达式]` 在捕获时创造一个新成员。735 行把可调用对象和 `promise` 移进任务，2072 行把初始化函数移进适配 lambda。这是把只可移动对象放进 lambda 的常用写法；名字会遮蔽外层同名变量。 |
| `mutable` lambda | `mutable` lambda | [include/BS_thread_pool.hpp:735](include/BS_thread_pool.hpp#L735) | 默认情况下 `operator()` 是 const，不能修改按值捕获的成员，也不能调用非 const 的 `operator()`。735 行的 `mutable` 允许移动 `promise` 并调用任务。`mutable` 不改变捕获本身是副本这一事实。 |
| 捕获 `this` | capturing `this` | [include/BS_thread_pool.hpp:2303](include/BS_thread_pool.hpp#L2303)；[tests/BS_thread_pool_test.cpp:158](tests/BS_thread_pool_test.cpp#L158) | `[this]` 捕获当前对象指针，lambda 里可直接用成员。2303 行谓词读取 `paused`、`tasks`；测试 158 行同样访问信号量成员。对象析构后调用该 lambda 是未定义行为。C++17 的 `[*this]` 才是捕获对象副本。 |

## 类与特殊成员函数

| 特性术语 | 英文术语 | 所在文件行号 | 语法说明 |
| --- | --- | --- | --- |
| `class` / `struct` | class and struct | [include/BS_thread_pool.hpp:143](include/BS_thread_pool.hpp#L143) [include/BS_thread_pool.hpp:312](include/BS_thread_pool.hpp#L312) [include/BS_thread_pool.hpp:459](include/BS_thread_pool.hpp#L459) [include/BS_thread_pool.hpp:1428](include/BS_thread_pool.hpp#L1428) [include/BS_thread_pool.hpp:2409](include/BS_thread_pool.hpp#L2409) | 两者都定义类类型，唯一默认差别是 `struct` 成员默认 public，`class` 默认 private。`version`、`pr_task` 用 `struct`，`thread_pool`、`move_only_function` 用 `class`。选择只影响默认访问；写了访问说明符之后二者能力相同。 |
| 访问控制 `public` / `private` | access specifiers | [include/BS_thread_pool.hpp:314](include/BS_thread_pool.hpp#L314) [include/BS_thread_pool.hpp:330](include/BS_thread_pool.hpp#L330) [include/BS_thread_pool.hpp:1430](include/BS_thread_pool.hpp#L1430) [include/BS_thread_pool.hpp:2052](include/BS_thread_pool.hpp#L2052) | `public` 谁都能用，`private` 只有本类和友元能用。接口放在 public，类型擦除的概念类和线程池内部队列放在 private。访问控制是编译期检查，不是运行期安全边界；指针偏移仍能触碰内存。 |
| 公有继承 | public inheritance | [include/BS_thread_pool.hpp:459](include/BS_thread_pool.hpp#L459) [include/BS_thread_pool.hpp:774](include/BS_thread_pool.hpp#L774) | `class D : public B` 表示 D 是 B，外部可把 D 当 B 用。`multi_future` 公有继承 `vector<future<T>>` 以复用容器接口；异常类公有继承 `runtime_error` 才能被 `catch (const std::exception&)` 接住。继承容器时基类析构往往不是 virtual，不要用基类指针删除派生对象。 |
| 继承构造函数 | inheriting constructors | [include/BS_thread_pool.hpp:463](include/BS_thread_pool.hpp#L463) | `using 基类::基类;` 把基类构造函数引入派生类。`multi_future` 因此能像 `vector` 一样用元素个数或迭代器构造。派生类自己的成员只做默认初始化；需要额外初始化时仍要手写构造函数。 |
| 嵌套类 | nested class | [include/BS_thread_pool.hpp:331](include/BS_thread_pool.hpp#L331) [include/BS_thread_pool.hpp:338](include/BS_thread_pool.hpp#L338) | 定义在另一个类内部的类，名字受外层访问控制约束。`func_concept` 和 `func_model` 藏在 `move_only_function` 里，只为类型擦除服务。嵌套类没有指向外层对象的隐式指针，要访问外层实例必须自己持有指针。 |
| `virtual` 析构函数 | virtual destructor | [include/BS_thread_pool.hpp:333](include/BS_thread_pool.hpp#L333) | 基类析构为 virtual 时，通过基类指针 `delete` 会先调用派生析构。`func_concept` 被 `unique_ptr` 持有，析构必须是 virtual，否则只销毁基类部分。一旦析构是 virtual，该类就不再是平凡可复制类型。 |
| 纯虚函数 | pure virtual function | [include/BS_thread_pool.hpp:334](include/BS_thread_pool.hpp#L334) | `= 0` 的虚函数没有必须在本类实现的版本，含有它的类是抽象类，不能直接构造。`call` 强制每个 `func_model` 提供自己的调用。抽象类可以有其他已实现的成员；派生类必须覆盖所有纯虚函数才能实例化。 |
| `override` | `override` | [include/BS_thread_pool.hpp:343](include/BS_thread_pool.hpp#L343) | 标明该函数覆盖基类虚函数，签名不一致时编译失败。`func_model::call` 覆盖 `func_concept::call`。没有 `override` 时，写错 const 或参数会变成一个新函数，基类版本仍被调用。 |
| `final` | `final` | [include/BS_thread_pool.hpp:338](include/BS_thread_pool.hpp#L338) | `final` 禁止再派生，或禁止再覆盖某个虚函数。`func_model` 标成 `final`，因为类型擦除不需要更深的继承。这也能让编译器去虚拟化调用。不要用在还打算扩展的接口上。 |
| 类型擦除 | type erasure | [include/BS_thread_pool.hpp:331-358](include/BS_thread_pool.hpp#L331) | 把不同具体类型收成同一个对外类型，内部用虚函数分派。`unique_ptr<func_concept>` 指向按实际可调用对象实例化的 `func_model<F>`，于是任意匹配签名的函数都能放进 `task_t`。会有一次堆分配和间接调用；小对象优化本 polyfill 没有做。 |
| 显式默认特殊成员 `= default` | explicitly defaulted special member functions | [include/BS_thread_pool.hpp:315-320](include/BS_thread_pool.hpp#L315)；[tests/BS_thread_pool_test.cpp:139](tests/BS_thread_pool_test.cpp#L139) [tests/BS_thread_pool_test.cpp:855](tests/BS_thread_pool_test.cpp#L855) | `= default` 让编译器按成员生成该特殊函数，并允许标 `noexcept` 或放在 public。315–320 行默认了移动和析构。用户声明了析构或其他特殊成员后，某些移动/拷贝不再隐式生成，需要的那些要显式 `= default`。 |
| 显式删除特殊成员 `= delete` | explicitly deleted special member functions | [include/BS_thread_pool.hpp:318-319](include/BS_thread_pool.hpp#L318) [include/BS_thread_pool.hpp:1487-1490](include/BS_thread_pool.hpp#L1487)；[tests/BS_thread_pool_test.cpp:135-138](tests/BS_thread_pool_test.cpp#L135) | `= delete` 禁止使用该函数，调用或重载选中它都会编译失败。拷贝构造和拷贝赋值被删掉，线程池和信号量也不能复制。删除比“声明为 private 且不定义”更明确，而且重载决议仍会看到它。 |
| 仅可移动类型 | move-only type | [include/BS_thread_pool.hpp:316-319](include/BS_thread_pool.hpp#L316) | 能移动、不能拷贝的类型，用来表示唯一所有权。`move_only_function` 默认移动、删除拷贝，因此任务队列只能把任务移走。标准容器在重分配时会移动它；需要拷贝的接口（如旧式 `std::function`）接不住它。 |
| 右值引用 | rvalue reference | [include/BS_thread_pool.hpp:316](include/BS_thread_pool.hpp#L316) [include/BS_thread_pool.hpp:417](include/BS_thread_pool.hpp#L417) [include/BS_thread_pool.hpp:1472](include/BS_thread_pool.hpp#L1472) | `T&&` 在类型已确定时只绑定右值（将要销毁或显式移出的对象）。移动构造和 `pr_task` 用 `task_t&&` 接管任务。不要把具名的右值引用当成右值：它是左值，再交给别人时要 `std::move`。 |
| 移动语义 `std::move` | move semantics | [include/BS_thread_pool.hpp:417](include/BS_thread_pool.hpp#L417) [include/BS_thread_pool.hpp:1559](include/BS_thread_pool.hpp#L1559) [include/BS_thread_pool.hpp:2255](include/BS_thread_pool.hpp#L2255) | `std::move` 只是把对象转成右值引用，真正的资源转移发生在移动构造或移动赋值。417 行把任务移进 `pr_task`，1559、2255 行把任务移出队列。移后源对象仍存在，但只能安全地销毁或重新赋值，不要再读它的旧值。 |
| `mutable` 数据成员 | `mutable` data member | [include/BS_thread_pool.hpp:434](include/BS_thread_pool.hpp#L434) [include/BS_thread_pool.hpp:2341](include/BS_thread_pool.hpp#L2341) [include/BS_thread_pool.hpp:2502](include/BS_thread_pool.hpp#L2502)；[tests/BS_thread_pool_test.cpp:243](tests/BS_thread_pool_test.cpp#L243) | `mutable` 成员即使在 const 对象或 const 成员函数里也能改。434 行因为 `priority_queue::top()` 返回 const 引用，却要把任务移出来；2341、2502 行的互斥量要在 const 观察函数里加锁。逻辑上仍应保持 const 承诺，不要用它偷偷改对外可见状态。 |
| `const` 成员函数 | const member function | [include/BS_thread_pool.hpp:182](include/BS_thread_pool.hpp#L182) [include/BS_thread_pool.hpp:493](include/BS_thread_pool.hpp#L493) [include/BS_thread_pool.hpp:509](include/BS_thread_pool.hpp#L509) | 声明在参数列表后的 `const` 使 `this` 成为 const 指针，不能改非 mutable 成员。`to_string`、`ready_count`、`valid` 只读取状态。const 对象只能调用 const 重载；需要修改时提供非 const 重载。 |
| `static` 成员函数 | static member function | [include/BS_thread_pool.hpp:977](include/BS_thread_pool.hpp#L977) [include/BS_thread_pool.hpp:2125](include/BS_thread_pool.hpp#L2125)；[tests/BS_thread_pool_test.cpp:146](tests/BS_thread_pool_test.cpp#L146) [tests/BS_thread_pool_test.cpp:1344](tests/BS_thread_pool_test.cpp#L1344) | 没有 `this` 的类函数，用 `类名::函数` 调用，所有对象共享。`this_thread::get_index` 读的是 thread_local，不是某个池对象的成员。它不能是 virtual，也不能直接访问非静态成员。 |
| 成员函数指针 | pointer to member function | [tests/BS_thread_pool_test.cpp:1369](tests/BS_thread_pool_test.cpp#L1369) | 类型写成 `返回类型 (类::*)(参数)`，指向某个成员函数，调用时还要提供对象。测试用它检查线程池能否提交成员函数。它不是普通函数指针，不能直接当回调；要用 `.*`、`->*` 或 `std::invoke`。 |
| 用户自定义拷贝 / 移动构造 | user-provided copy and move constructors | [tests/BS_thread_pool_test.cpp:857-859](tests/BS_thread_pool_test.cpp#L857) | 自己写出的拷贝或移动构造会抑制编译器生成其余特殊成员，并可能让类不再平凡。测试用自定义版本观察任务被复制还是被移动。若只写了拷贝构造，移动可能退化为拷贝；写了就要同时考虑拷贝赋值、移动赋值和析构。 |

## 模板与编译期分支

| 特性术语 | 英文术语 | 所在文件行号 | 语法说明 |
| --- | --- | --- | --- |
| 函数模板 / 类模板 | function template / class template | [include/BS_thread_pool.hpp:312](include/BS_thread_pool.hpp#L312) [include/BS_thread_pool.hpp:458](include/BS_thread_pool.hpp#L458) [include/BS_thread_pool.hpp:534](include/BS_thread_pool.hpp#L534) [include/BS_thread_pool.hpp:1427](include/BS_thread_pool.hpp#L1427) [include/BS_thread_pool.hpp:2458](include/BS_thread_pool.hpp#L2458) | 模板按使用时的实参生成具体函数或类。`move_only_function<R(Args...)>`、`multi_future<T>`、`thread_pool<OptFlags>` 都是类模板，打印函数是函数模板。定义通常要放在头文件里；不同实参是不同类型，不会自动互相转换。 |
| 成员函数模板 | member function template | [include/BS_thread_pool.hpp:322](include/BS_thread_pool.hpp#L322) [include/BS_thread_pool.hpp:1530](include/BS_thread_pool.hpp#L1530) [include/BS_thread_pool.hpp:2063](include/BS_thread_pool.hpp#L2063) | 类的某个成员自己再带模板参数，用来接受任意符合条件的实参。322 行构造函数接受任意可调用对象 `F`，2063 行接受任意初始化函数。成员模板不能是 virtual；它和普通构造函数重载时，要注意不要把拷贝/移动构造“吃掉”。 |
| 变参模板 | variadic template | [include/BS_thread_pool.hpp:302](include/BS_thread_pool.hpp#L302) [include/BS_thread_pool.hpp:311](include/BS_thread_pool.hpp#L311) [include/BS_thread_pool.hpp:2426](include/BS_thread_pool.hpp#L2426) [include/BS_thread_pool.hpp:2458](include/BS_thread_pool.hpp#L2458)；[tests/BS_thread_pool_test.cpp:370](tests/BS_thread_pool_test.cpp#L370) [tests/BS_thread_pool_test.cpp:599](tests/BS_thread_pool_test.cpp#L599) | `typename...` 表示零个或多个类型。`move_only_function` 的主模板用它做声明，`synced_stream` 的构造和 `print` 用它接受任意个流或待打印对象。包的长度要到实例化时才知道；空包需要折叠表达式或重载才能处理。 |
| 形参包展开 | parameter pack expansion | [include/BS_thread_pool.hpp:327](include/BS_thread_pool.hpp#L327) [include/BS_thread_pool.hpp:2475](include/BS_thread_pool.hpp#L2475)；[tests/BS_thread_pool_test.cpp:374](tests/BS_thread_pool_test.cpp#L374) [tests/BS_thread_pool_test.cpp:388](tests/BS_thread_pool_test.cpp#L388) | 在含包名的模式后写 `...`，把模式重复成逗号分隔的列表。327 行把 `Args` 转发给 `call`，2475 行把每个待打印项交给输出。展开位置必须是语法上允许重复的地方（函数实参、基类列表、初始化列表等）。 |
| 折叠表达式 | fold expression | [include/BS_thread_pool.hpp:2429](include/BS_thread_pool.hpp#L2429) [include/BS_thread_pool.hpp:2463](include/BS_thread_pool.hpp#L2463)；[tests/BS_thread_pool_test.cpp:602](tests/BS_thread_pool_test.cpp#L602) | C++17 用 `(模式 运算符 ...)` 把参数包收成一个表达式。2429 行 `(add_stream(streams), ...)` 对每个流调用一次，逗号运算符保证按顺序执行。一元折叠在包为空时，只有逻辑与、逻辑或、逗号三种有默认值；其他运算符的空包是病态的。 |
| 转发引用与完美转发 | forwarding reference and perfect forwarding | [include/BS_thread_pool.hpp:323](include/BS_thread_pool.hpp#L323) [include/BS_thread_pool.hpp:341](include/BS_thread_pool.hpp#L341) [include/BS_thread_pool.hpp:735](include/BS_thread_pool.hpp#L735) [include/BS_thread_pool.hpp:1472](include/BS_thread_pool.hpp#L1472) [include/BS_thread_pool.hpp:1632](include/BS_thread_pool.hpp#L1632)；[tests/BS_thread_pool_test.cpp:388](tests/BS_thread_pool_test.cpp#L388) | 模板参数的 `T&&` 既能绑定左值也能绑定右值，叫做转发引用。`std::forward<T>` 按原来的值类别再传出去，323、341、735 行因此既能复制也能移入可调用对象。只有形如 `T&&` 且 `T` 是被推导的模板参数时才是转发引用；`task_t&&` 只是普通右值引用。转发同一个参数两次会把已移走的对象再移一次。 |
| 非类型模板形参 | non-type template parameter | [include/BS_thread_pool.hpp:293](include/BS_thread_pool.hpp#L293) [include/BS_thread_pool.hpp:1427](include/BS_thread_pool.hpp#L1427) [include/BS_thread_pool.hpp:2157](include/BS_thread_pool.hpp#L2157)；[tests/BS_thread_pool_test.cpp:121](tests/BS_thread_pool_test.cpp#L121) | 模板参数可以是编译期值，而不只是类型。`thread_pool<tp::priority>` 的 `OptFlags`、测试信号量的 `LeastMaxValue` 都是值参数。不同值产生不同类型。C++17 起可用 `auto` 作非类型参数；字符串字面量直到 C++20 才比较好用。 |
| 默认模板实参 | default template argument | [include/BS_thread_pool.hpp:1427](include/BS_thread_pool.hpp#L1427) [include/BS_thread_pool.hpp:2157](include/BS_thread_pool.hpp#L2157)；[tests/BS_thread_pool_test.cpp:121](tests/BS_thread_pool_test.cpp#L121) | 模板参数可以从右向左给出默认值。`thread_pool<>` 等价于 `thread_pool<tp::none>`，信号量上限默认是 `ptrdiff_t` 的最大值。类模板可以在前向声明或定义处给默认值，但同一参数只能指定一次。 |
| 类模板前向声明 | class template forward declaration | [include/BS_thread_pool.hpp:293-294](include/BS_thread_pool.hpp#L293) [include/BS_thread_pool.hpp:302-303](include/BS_thread_pool.hpp#L302) | 只声明模板名字和参数，不给定义，以便后面的友元或指针先提到它。293 行先声明 `thread_pool`，302 行先声明主模板再在 312 行偏特化。前向声明之后只能用指针或引用，不能创建对象或访问成员。 |
| 类模板偏特化 | partial specialization | [include/BS_thread_pool.hpp:312](include/BS_thread_pool.hpp#L312) [include/BS_thread_pool.hpp:1367](include/BS_thread_pool.hpp#L1367) [include/BS_thread_pool.hpp:1374](include/BS_thread_pool.hpp#L1374) [include/BS_thread_pool.hpp:1381](include/BS_thread_pool.hpp#L1381) | 为主模板的一部分参数组合提供另一套定义。`move_only_function<R(Args...)>` 只匹配函数类型；`common_index_type` 分别为两个有符号、两个无符号整数给出特化。偏特化的参数必须比主模板更特殊；函数模板不能偏特化，只能重载。 |
| 别名模板 | alias template | [include/BS_thread_pool.hpp:1400](include/BS_thread_pool.hpp#L1400) | `template<...> using 名 = 类型;` 给依赖参数的类型起名字。1400 行把 `common_index_type<T1,T2>::type` 收成 `common_index_type_t`。别名不会被特化；要定制行为必须特化背后的类模板。 |
| SFINAE 与 `std::enable_if_t` | SFINAE with `std::enable_if_t` | [include/BS_thread_pool.hpp:322](include/BS_thread_pool.hpp#L322) [include/BS_thread_pool.hpp:340](include/BS_thread_pool.hpp#L340) [include/BS_thread_pool.hpp:449-450](include/BS_thread_pool.hpp#L449) [include/BS_thread_pool.hpp:730](include/BS_thread_pool.hpp#L730) [include/BS_thread_pool.hpp:1367](include/BS_thread_pool.hpp#L1367) | 替换失败不是错误：模板参数不合法时，这个候选被丢掉而不是编译失败。`enable_if_t<条件>` 在条件为假时没有 `type`，于是该构造或特化退出重载。322 行用它排除“用自身构造自身”，并要求可调用。错误信息往往很长；C++20 可用 `requires` 代替。条件要放在立即上下文里，放进函数体就不再是 SFINAE。 |
| 概念 `concept` | concept | [include/BS_thread_pool.hpp:445-446](include/BS_thread_pool.hpp#L445) | C++20 的 `concept` 是具名的编译期谓词，用来约束模板参数。`init_func_c` 要求类型能以零个参数或一个 `size_t` 调用。概念失败时错误指向约束本身，比 SFINAE 清楚。概念不产生代码，也不能在运行期求值。 |
| `requires` 子句 | requires-clause | [include/BS_thread_pool.hpp:444](include/BS_thread_pool.hpp#L444) | 写在模板头后面，条件为假则该模板不参与重载。444 行 `requires(P)` 让暂停相关成员只在 `pause_enabled` 为真时存在。`requires` 表达式（检查表达式是否合法）和 `requires` 子句不是同一个语法；这里用的是子句。 |
| 标准概念 `std::invocable` | standard concept `std::invocable` | [include/BS_thread_pool.hpp:446](include/BS_thread_pool.hpp#L446) | `<concepts>` 中的概念，判断类型能否用给定实参类型调用。446 行允许初始化函数是 `F()` 或 `F(size_t)`。它不检查返回类型；要约束返回值用 `std::invocable` 的兄弟 `std::predicate`，或本库 C++17 路径里的 `is_invocable_r_v`。 |
| `if constexpr` | `if constexpr` | [include/BS_thread_pool.hpp:345](include/BS_thread_pool.hpp#L345) [include/BS_thread_pool.hpp:472](include/BS_thread_pool.hpp#L472) [include/BS_thread_pool.hpp:741](include/BS_thread_pool.hpp#L741) [include/BS_thread_pool.hpp:1552](include/BS_thread_pool.hpp#L1552) [include/BS_thread_pool.hpp:1631](include/BS_thread_pool.hpp#L1631) [include/BS_thread_pool.hpp:1700](include/BS_thread_pool.hpp#L1700) [include/BS_thread_pool.hpp:2066](include/BS_thread_pool.hpp#L2066) | 条件是常量表达式时，没选中的分支被丢弃，不实例化。345、741 行按返回值是否为 `void` 分叉，472 行决定 `get()` 返回 `void` 还是 `vector`，1552 行按是否启用优先级选队列操作。丢弃分支里的语法仍要合法，但其中的模板不实例化。普通 `if` 两个分支都会做类型检查。 |
| 类型特征 `_v` / `_t` | type trait variable templates and alias templates | [include/BS_thread_pool.hpp:276](include/BS_thread_pool.hpp#L276) [include/BS_thread_pool.hpp:322](include/BS_thread_pool.hpp#L322) [include/BS_thread_pool.hpp:345](include/BS_thread_pool.hpp#L345) [include/BS_thread_pool.hpp:470](include/BS_thread_pool.hpp#L470) [include/BS_thread_pool.hpp:1362](include/BS_thread_pool.hpp#L1362) [include/BS_thread_pool.hpp:1367](include/BS_thread_pool.hpp#L1367) [include/BS_thread_pool.hpp:2066](include/BS_thread_pool.hpp#L2066) | `_t` 是特征的类型别名，`_v` 是其特征值的变量模板。`underlying_type_t`、`enable_if_t`、`decay_t`、`is_void_v`、`is_same_v` 用来在编译期问类型的性质。它们不转换对象，只产生类型或 `bool`。对不完整类型使用某些特征是未定义行为。 |
| std::invoke | std::invoke | [include/BS_thread_pool.hpp:347](include/BS_thread_pool.hpp#L347) [include/BS_thread_pool.hpp:351](include/BS_thread_pool.hpp#L351) | 用统一语法调用函数、函数指针、成员指针或函数对象。347、351 行在类型擦除的 `call` 里调用用户保存的 `stored_func`，不用关心它到底是哪种可调用体。成员指针必须把对象作为第一个实参。它不捕获异常，异常照常传播。 |
| std::conditional_t 选择成员类型 | std::conditional_t for member type selection | [include/BS_thread_pool.hpp:470](include/BS_thread_pool.hpp#L470) [include/BS_thread_pool.hpp:2371](include/BS_thread_pool.hpp#L2371) [include/BS_thread_pool.hpp:2391](include/BS_thread_pool.hpp#L2391) | `conditional_t<条件, A, B>` 在编译期二选一。470 行 `void` 任务的 `get()` 返回 `void`，否则返回 `vector<T>`；2371 行按优先级在 `priority_queue` 和 `queue` 之间选；2391 行未启用暂停时用 `monostate` 占位，避免多余的 `bool`。两个分支的类型都要写得合法，即使没被选中。 |

## 异常

| 特性术语 | 英文术语 | 所在文件行号 | 语法说明 |
| --- | --- | --- | --- |
| `try` / `catch` | `try` / `catch` | [include/BS_thread_pool.hpp:738-761](include/BS_thread_pool.hpp#L738) [include/BS_thread_pool.hpp:1498-1509](include/BS_thread_pool.hpp#L1498) [include/BS_thread_pool.hpp:2317-2326](include/BS_thread_pool.hpp#L2317) | `try` 里抛出的异常按 `catch` 的类型从上到下匹配，匹配后该异常被视为已处理。738 行包住任务执行并把结果或异常写入 promise，2317 行包住工作线程里的任务以免线程函数逃出异常。异常会展开栈并析构成员；`noexcept` 函数里抛出则直接终止。关闭异常编译时这些块被宏去掉。 |
| 捕获所有异常 `catch (...)` | catch-all handler | [include/BS_thread_pool.hpp:752](include/BS_thread_pool.hpp#L752) [include/BS_thread_pool.hpp:758](include/BS_thread_pool.hpp#L758) [include/BS_thread_pool.hpp:1507](include/BS_thread_pool.hpp#L1507) [include/BS_thread_pool.hpp:2323](include/BS_thread_pool.hpp#L2323) | `catch (...)` 接住任何类型，包括不是 `std::exception` 的对象。752 行接住任务异常再转交 promise，758 行防止 `set_exception` 自己再抛，2323 行吞掉工作线程中的异常以免 `std::terminate`。它会隐藏错误；库边界可以这样隔离，业务代码通常应至少记录或重新抛出。 |
| 自定义异常类 | user-defined exception type | [include/BS_thread_pool.hpp:774](include/BS_thread_pool.hpp#L774) | 自己的异常类型最好公有继承 `std::exception` 或其派生类，这样现有的 `catch (const std::exception&)` 能接住。本库用它表示等待死锁。按引用捕获，不要按值，否则会切片。析构不应再抛异常。 |
| `std::runtime_error` | `std::runtime_error` | [include/BS_thread_pool.hpp:774-776](include/BS_thread_pool.hpp#L774) | 标准异常，表示运行期才能发现的错误，构造时带一条 `string` 消息。死锁异常继承它，调用 `what()` 能拿到说明。它不是逻辑错误（`logic_error`）；不要用异常做正常控制流。 |
| `std::exception_ptr` 与 `std::current_exception` | `std::exception_ptr` and `std::current_exception` | [include/BS_thread_pool.hpp:756](include/BS_thread_pool.hpp#L756) | `current_exception()` 只能在 `catch` 里调用，返回指向当前异常的 `exception_ptr`，可跨线程保存。756 行把它交给 promise，稍后 `future::get` 在另一处重新抛出。空的 `exception_ptr` 表示没有异常；重新抛出用 `std::rethrow_exception`。 |
| `std::promise::set_exception` / `set_value` | promise exception and value propagation | [include/BS_thread_pool.hpp:744](include/BS_thread_pool.hpp#L744) [include/BS_thread_pool.hpp:748](include/BS_thread_pool.hpp#L748) [include/BS_thread_pool.hpp:756](include/BS_thread_pool.hpp#L756) | `promise` 是一次性的结果通道：`set_value` 写入返回值，`set_exception` 写入异常，对应的 `future::get` 取到值或重新抛出。744 行对 `void` 调用无实参的 `set_value`，748 行写入返回值，756 行写入异常。同一个 promise 只能设置一次，第二次会抛 `future_error`。 |

## 并发与同步

| 特性术语 | 英文术语 | 所在文件行号 | 语法说明 |
| --- | --- | --- | --- |
| `std::thread` | `std::thread` | [include/BS_thread_pool.hpp:381](include/BS_thread_pool.hpp#L381) | C++11 的线程句柄，构造时启动新线程执行可调用对象。没有 `jthread` 时 `thread_t` 就是它。析构前必须 `join` 或 `detach`，否则 `std::terminate`。它不可拷贝，只可移动。 |
| `std::jthread`（C++20） | `std::jthread` | [include/BS_thread_pool.hpp:371](include/BS_thread_pool.hpp#L371) | 析构时自动请求停止并 `join` 的线程。有 `__cpp_lib_jthread` 时工作线程用它，池的析构不必手写 join。仍不可拷贝。若线程函数不接受 `stop_token`，析构只会 join，不会协作式停止。 |
| `std::stop_token`（C++20） | `std::stop_token` | [include/BS_thread_pool.hpp:373](include/BS_thread_pool.hpp#L373) [include/BS_thread_pool.hpp:375](include/BS_thread_pool.hpp#L375) [include/BS_thread_pool.hpp:2091](include/BS_thread_pool.hpp#L2091) | 只读的停止查询句柄，`stop_requested()` 表示已请求停止。373 行用宏把 `const stop_token&` 插进工作函数签名，2091 行的线程入口接收它。`jthread` 会把 token 作为第一个参数传入；不要按值长期保存后又假设它和线程同寿。 |
| `hardware_concurrency` | `std::thread::hardware_concurrency` | [include/BS_thread_pool.hpp:2137-2138](include/BS_thread_pool.hpp#L2137)；[tests/BS_thread_pool_test.cpp:817](tests/BS_thread_pool_test.cpp#L817) | 返回实现建议的并发线程数，通常接近逻辑处理器个数，无法确定时返回 0。线程池用它作为默认线程数。它不是实时的负载，也不保证等于进程亲和性掩码里的 CPU 数；本库在原生扩展下会改用亲和性。 |
| `native_handle` | `native_handle` | [include/BS_thread_pool.hpp:1645-1649](include/BS_thread_pool.hpp#L1645) | 返回底层线程句柄（Windows 的 `HANDLE`，POSIX 的 `pthread_t`），以便调用系统 API。类型由实现定义，不可移植。句柄的所有权仍在 `thread` 对象上，不要用它 `CloseHandle` 或 `pthread_detach`。 |
| `std::thread::join` | `join` | [include/BS_thread_pool.hpp:2116](include/BS_thread_pool.hpp#L2116) | 阻塞到该线程函数返回，然后线程句柄变为不可 join。C++17 路径在销毁池时对每个 worker `join`。对不可 join 的线程调用是未定义行为；不要在被 join 的那个线程里 join 自己。 |
| `std::mutex` | `std::mutex` | [include/BS_thread_pool.hpp:2341](include/BS_thread_pool.hpp#L2341) [include/BS_thread_pool.hpp:2502](include/BS_thread_pool.hpp#L2502)；[tests/BS_thread_pool_test.cpp:243](tests/BS_thread_pool_test.cpp#L243) | 互斥量保证同一时刻只有一个线程持有锁。任务队列和同步输出流各有一把。不可拷贝、不可移动。同一线程对普通 `mutex` 再次加锁是未定义行为；需要重入要用 `recursive_mutex`。 |
| `std::scoped_lock` 与 CTAD | `std::scoped_lock` and class template argument deduction | [include/BS_thread_pool.hpp:1551](include/BS_thread_pool.hpp#L1551) [include/BS_thread_pool.hpp:1630](include/BS_thread_pool.hpp#L1630) [include/BS_thread_pool.hpp:2080](include/BS_thread_pool.hpp#L2080) [include/BS_thread_pool.hpp:2461](include/BS_thread_pool.hpp#L2461)；[tests/BS_thread_pool_test.cpp:173](tests/BS_thread_pool_test.cpp#L173) [tests/BS_thread_pool_test.cpp:186](tests/BS_thread_pool_test.cpp#L186) | RAII 锁，构造加锁、析构解锁，并能一次锁多把且避免死锁。`scoped_lock tasks_lock(tasks_mutex)` 靠 CTAD 推导模板参数，不必写 `scoped_lock<mutex>`。它不能中途 `unlock`；需要和条件变量配合时用 `unique_lock`。 |
| `std::unique_lock` 与 CTAD | `std::unique_lock` and CTAD | [include/BS_thread_pool.hpp:2290](include/BS_thread_pool.hpp#L2290)；[tests/BS_thread_pool_test.cpp:156](tests/BS_thread_pool_test.cpp#L156) [tests/BS_thread_pool_test.cpp:206](tests/BS_thread_pool_test.cpp#L206) | 可移动的 RAII 锁，能延迟加锁、提前解锁，并能交给 `condition_variable::wait`。2290 行在等待任务前持有队列锁，`wait` 会原子地解锁并再加锁。比 `scoped_lock` 更灵活，也稍重。不要在已解锁后再使用它保护的数据。 |
| `std::condition_variable` | `std::condition_variable` | [include/BS_thread_pool.hpp:2349](include/BS_thread_pool.hpp#L2349) [include/BS_thread_pool.hpp:2356](include/BS_thread_pool.hpp#L2356)；[tests/BS_thread_pool_test.cpp:248](tests/BS_thread_pool_test.cpp#L248) | 让线程在某个条件成立前睡眠，必须配合 `unique_lock<mutex>`。一个用于“有任务”，一个用于“任务都做完”。等待有虚假唤醒，所以条件要放在谓词或循环里检查。通知不是记住的：没有等待者时 `notify` 会丢失。 |
| `std::condition_variable_any` | `std::condition_variable_any` | [include/BS_thread_pool.hpp:2347](include/BS_thread_pool.hpp#L2347) | 能和任意满足 Lockable 的锁配合，而不只是 `unique_lock<mutex>`。在需要和 `stop_token` 一起等待时使用。它比 `condition_variable` 开销更大；锁只是普通 `mutex` 时优先用后者。 |
| 带谓词的 `wait` / `wait_for` / `wait_until` | condition variable wait with predicate | [include/BS_thread_pool.hpp:2302-2309](include/BS_thread_pool.hpp#L2302)；[tests/BS_thread_pool_test.cpp:157](tests/BS_thread_pool_test.cpp#L157) [tests/BS_thread_pool_test.cpp:207](tests/BS_thread_pool_test.cpp#L207) [tests/BS_thread_pool_test.cpp:229](tests/BS_thread_pool_test.cpp#L229) | `wait(lock, pred)` 在谓词为假时才睡眠，被唤醒后重新加锁并再检查谓词，从而处理虚假唤醒。2303 行的谓词是“未暂停且队列非空，或已请求停止”。谓词必须在持锁时判断共享状态。`wait_for` / `wait_until` 超时后返回谓词的当前值，超时不一定表示条件为真。 |
| `notify_one` / `notify_all` | `notify_one` / `notify_all` | [include/BS_thread_pool.hpp:1565](include/BS_thread_pool.hpp#L1565) [include/BS_thread_pool.hpp:1636](include/BS_thread_pool.hpp#L1636) [include/BS_thread_pool.hpp:2114](include/BS_thread_pool.hpp#L2114) [include/BS_thread_pool.hpp:2295](include/BS_thread_pool.hpp#L2295)；[tests/BS_thread_pool_test.cpp:176](tests/BS_thread_pool_test.cpp#L176) | `notify_one` 唤醒一个等待者，`notify_all` 唤醒全部。新任务用 `notify_one`，收尾或停止用 `notify_all`。应在修改条件之后通知；可以在解锁之后通知以减少被唤醒线程立刻再次阻塞。通知不传递数据，等待者必须自己看共享状态。 |
| `std::atomic` | `std::atomic` | [tests/BS_thread_pool_test.cpp:790](tests/BS_thread_pool_test.cpp#L790) [tests/BS_thread_pool_test.cpp:1327](tests/BS_thread_pool_test.cpp#L1327) [tests/BS_thread_pool_test.cpp:1432](tests/BS_thread_pool_test.cpp#L1432) [tests/BS_thread_pool_test.cpp:2412](tests/BS_thread_pool_test.cpp#L2412) | 对整数或指针的读改写是不可分割的，测试用它做跨线程计数。默认内存序是 `seq_cst`，最安全也最重。`atomic` 不可拷贝；复合操作要用 `fetch_add` 等成员，普通 `x = x + 1` 不是原子的。 |
| `std::future` / `std::promise` | `std::future` / `std::promise` | [include/BS_thread_pool.hpp:459](include/BS_thread_pool.hpp#L459) [include/BS_thread_pool.hpp:733-766](include/BS_thread_pool.hpp#L733)；[tests/BS_thread_pool_test.cpp:970](tests/BS_thread_pool_test.cpp#L970) | `promise` 在生产者一侧写入结果，`get_future()` 得到消费者一侧的 `future`。`multi_future` 是 `vector<future<T>>`，`task_and_future` 在提交时配对二者。二者都只可移动。`future` 若在析构时仍关联共享状态且是最后一个引用，不会等待任务完成。 |
| `std::future_status` | `std::future_status` | [include/BS_thread_pool.hpp:498](include/BS_thread_pool.hpp#L498) | `wait_for` / `wait_until` 的返回值：`ready`、`timeout`、`deferred`。498 行用零时长的 `wait_for` 探测 future 是否已就绪，等于 `ready` 才计数。`deferred` 表示任务要到 `get`/`wait` 才在本线程执行；本库提交到线程池的 future 不会是 deferred。 |
| `future::get` / `wait` / `wait_for` / `wait_until` | future waiting and result retrieval | [include/BS_thread_pool.hpp:475](include/BS_thread_pool.hpp#L475) [include/BS_thread_pool.hpp:498](include/BS_thread_pool.hpp#L498) [include/BS_thread_pool.hpp:523](include/BS_thread_pool.hpp#L523) [include/BS_thread_pool.hpp:540](include/BS_thread_pool.hpp#L540) [include/BS_thread_pool.hpp:560](include/BS_thread_pool.hpp#L560) | `get` 阻塞到结果就绪，返回值并在有异常时重新抛出，且只能调用一次。`wait` 只等待不取值，可重复。`wait_for` / `wait_until` 带超时。对无效 future（已经 `get` 过或默认构造）调用这些函数会抛异常。 |
| `std::counting_semaphore` / `std::binary_semaphore`（C++20） | counting and binary semaphore | [tests/BS_thread_pool_test.cpp:113-114](tests/BS_thread_pool_test.cpp#L113) [tests/BS_thread_pool_test.cpp:121](tests/BS_thread_pool_test.cpp#L121) [tests/BS_thread_pool_test.cpp:259](tests/BS_thread_pool_test.cpp#L259) [tests/BS_thread_pool_test.cpp:1083](tests/BS_thread_pool_test.cpp#L1083) | 计数信号量用内部计数限制并发：`acquire` 减一，为零则阻塞，`release` 加一。`binary_semaphore` 是最大值为 1 的别名。没有 C++20 时测试在 121 行用互斥量和条件变量做了 polyfill。不要把信号量当带数据的队列；它只计数。 |
| `thread_local` | `thread_local` | [include/BS_thread_pool.hpp:1347-1348](include/BS_thread_pool.hpp#L1347) | 每个线程各有一份对象，线程首次经过声明时初始化，线程结束时析构。用来记住“当前线程属于哪个池、下标是多少”。不同线程的副本互不可见。线程池复用线程时，这份状态会一直留到该 worker 退出。 |
| `inline static thread_local` | inline static thread-local variable | [include/BS_thread_pool.hpp:1347-1348](include/BS_thread_pool.hpp#L1347) | `static` 使它属于类而不是对象，`inline` 允许在头文件里定义，`thread_local` 使每线程一份。`my_index` 和 `my_pool` 因此是全程序共享的“每线程槽”，而不是每个 `this_thread` 对象一份。三个说明符缺一不可，否则要么 ODR，要么变成普通成员。 |

## 标准库组件

| 特性术语 | 英文术语 | 所在文件行号 | 语法说明 |
| --- | --- | --- | --- |
| `std::move_only_function`（C++23） | `std::move_only_function` | [include/BS_thread_pool.hpp:300](include/BS_thread_pool.hpp#L300)；[tests/BS_thread_pool_test.cpp:1394](tests/BS_thread_pool_test.cpp#L1394) | 只可移动的类型擦除函数包装，能装带移动捕获的 lambda。有该特性时 `task_t` 直接用它；否则用前面的 polyfill。它的 `operator()` 不是 const。空的对象调用是未定义行为。 |
| `std::function` | `std::function` | [tests/BS_thread_pool_test.cpp:1384](tests/BS_thread_pool_test.cpp#L1384) | 可拷贝的类型擦除函数包装。测试用它提交可复制的回调。目标必须可拷贝，所以装不进只可移动的 lambda；那种情况用 `move_only_function`。调用空的 `function` 会抛 `bad_function_call`。 |
| `std::bind` / `std::ref` | `std::bind` / `std::ref` | [tests/BS_thread_pool_test.cpp:1410](tests/BS_thread_pool_test.cpp#L1410) | `bind` 把函数和部分实参绑成新的可调用对象；`ref` 包一层引用包装，避免 `bind` 复制实参。测试用来提交带绑定参数的任务。`bind` 占位符和值类别容易出错，新代码更常写 lambda。`ref` 不延长对象寿命。 |
| `std::optional` / `std::nullopt` | `std::optional` / `std::nullopt` | [include/BS_thread_pool.hpp:842](include/BS_thread_pool.hpp#L842) [include/BS_thread_pool.hpp:848](include/BS_thread_pool.hpp#L848) [include/BS_thread_pool.hpp:913](include/BS_thread_pool.hpp#L913) [include/BS_thread_pool.hpp:977](include/BS_thread_pool.hpp#L977) [include/BS_thread_pool.hpp:1248](include/BS_thread_pool.hpp#L1248) [include/BS_thread_pool.hpp:1347](include/BS_thread_pool.hpp#L1347) [include/BS_thread_pool.hpp:2130](include/BS_thread_pool.hpp#L2130) | 要么含一个 `T`，要么为空。亲和性查不到、线程不属于任何池时返回 `nullopt`。空的 optional 不是“值为零的 T”。不要用它表示错误详情，那种情况更适合异常或 `expected`。 |
| `optional::has_value` / `value` | `optional::has_value` / `value` | [include/BS_thread_pool.hpp:1294-1295](include/BS_thread_pool.hpp#L1294) [include/BS_thread_pool.hpp:2131](include/BS_thread_pool.hpp#L2131) | `has_value` 询问是否有值；`value` 取出值，为空则抛 `bad_optional_access`。先判断再取，或用 `value_or` 给默认值。`operator*` 在为空时是未定义行为，不会抛异常。 |
| `std::monostate` | `std::monostate` | [include/BS_thread_pool.hpp:2391](include/BS_thread_pool.hpp#L2391) | 空类型，所有实例都相等，常用作 `variant` 的第一个选项。未启用暂停时 `paused` 的类型是它，对象不占有效状态，`{}` 就能构造。它不是 `bool`，不能写 `if (paused)`，所以那一支代码被 `if constexpr` 丢掉。 |
| `std::tuple` 与 CTAD | `std::tuple` and CTAD | [include/BS_thread_pool.hpp:153](include/BS_thread_pool.hpp#L153) [include/BS_thread_pool.hpp:163](include/BS_thread_pool.hpp#L163) [include/BS_thread_pool.hpp:173](include/BS_thread_pool.hpp#L173) | `tuple` 把多个值捆成一个对象，并已定义字典序比较。C++17 类模板实参推导让 `std::tuple(a, b, c)` 不必写出类型。151 行起用它比较三个版本字段。元组比较是按顺序的，字段顺序就是优先级。 |
| `std::pair` | `std::pair` | [tests/BS_thread_pool_test.cpp:572](tests/BS_thread_pool_test.cpp#L572) [tests/BS_thread_pool_test.cpp:1669](tests/BS_thread_pool_test.cpp#L1669) | 正好两个成员 `first`、`second` 的结构。测试用它成对保存输入和期望输出。需要名字时用结构体更清楚；只要两个值时 `pair` 更短。C++17 可用 `std::pair{a, b}` 推导类型。 |
| `std::string` / `std::wstring` / `std::to_string` | `std::string` / `std::wstring` / `std::to_string` | [include/BS_thread_pool.hpp:182-184](include/BS_thread_pool.hpp#L182) [include/BS_thread_pool.hpp:1090](include/BS_thread_pool.hpp#L1090) [include/BS_thread_pool.hpp:1124](include/BS_thread_pool.hpp#L1124) | `string` 是拥有字符缓冲区的动态字符串，`to_string` 把算术值转成十进制文本。`version::to_string` 把三段整数拼成 `"5.1.0"`。`wstring` 用于宽字符 API。`to_string` 不接受 `string` 自身，也不做本地化格式。 |
| `std::string_view`（C++17） | `std::string_view` | [tests/BS_thread_pool_test.cpp:427](tests/BS_thread_pool_test.cpp#L427) [tests/BS_thread_pool_test.cpp:441](tests/BS_thread_pool_test.cpp#L441) [tests/BS_thread_pool_test.cpp:885](tests/BS_thread_pool_test.cpp#L885) [tests/BS_thread_pool_test.cpp:2308](tests/BS_thread_pool_test.cpp#L2308) | 不拥有字符的只读视图，可指向 `string` 或字符串字面量，避免复制。测试用它传消息文本。它不延长源字符串的寿命；源是临时 `string` 时，视图会立刻悬空。 |
| `std::initializer_list` | `std::initializer_list` | [tests/BS_thread_pool_test.cpp:317](tests/BS_thread_pool_test.cpp#L317) [tests/BS_thread_pool_test.cpp:347](tests/BS_thread_pool_test.cpp#L347) [tests/BS_thread_pool_test.cpp:399](tests/BS_thread_pool_test.cpp#L399) | 花括号列表背后的轻量数组代理，元素是 const。测试用 `{...}` 把一组参数交给接受 `initializer_list` 的接口。它按 const 引用指向临时数组，不要保存这个 list 本身超出完整表达式。 |
| `std::vector` / `std::vector<bool>` | `std::vector` / `std::vector<bool>` | [include/BS_thread_pool.hpp:459](include/BS_thread_pool.hpp#L459) [include/BS_thread_pool.hpp:480](include/BS_thread_pool.hpp#L480) [include/BS_thread_pool.hpp:842](include/BS_thread_pool.hpp#L842) [include/BS_thread_pool.hpp:1647](include/BS_thread_pool.hpp#L1647) [include/BS_thread_pool.hpp:2201](include/BS_thread_pool.hpp#L2201) | 动态连续数组，增长时可能重新分配并使迭代器和引用失效。`multi_future` 继承 `vector<future<T>>`，亲和性用 `vector<bool>`。`vector<bool>` 不是真正的容器：`operator[]` 返回代理而不是 `bool&`，不能取元素地址。 |
| `std::queue` / `std::priority_queue` | `std::queue` / `std::priority_queue` | [include/BS_thread_pool.hpp:2371](include/BS_thread_pool.hpp#L2371) | 适配器：`queue` 是 FIFO，`priority_queue` 每次 `top` 是当前最大元素。启用优先级时任务队列是 `priority_queue<pr_task>`，否则是 `queue<task_t>`。`priority_queue` 没有遍历接口，`top` 是 const，所以 `pr_task::task` 标了 `mutable` 才能移出。比较器必须是严格弱序。 |
| `std::array` | `std::array` | [tests/BS_thread_pool_test.cpp:3304](tests/BS_thread_pool_test.cpp#L3304) [tests/BS_thread_pool_test.cpp:3488](tests/BS_thread_pool_test.cpp#L3488) | 固定长度、存在栈上（作为对象成员时）的数组，知道自己的大小，能用范围 for。长度是类型的一部分，`array<int,3>` 和 `array<int,4>` 不同。越界的 `operator[]` 不检查；要检查用 `at`。 |
| `std::map` | `std::map` | [tests/BS_thread_pool_test.cpp:2794](tests/BS_thread_pool_test.cpp#L2794) [tests/BS_thread_pool_test.cpp:3967](tests/BS_thread_pool_test.cpp#L3967) | 按键有序的关联容器，通常是红黑树，查找是对数时间。测试用它汇总结果。键必须能 `<` 比较，且插入后不要改键。只查存在性、不需要顺序时 `unordered_map` 平均更快。 |
| `std::unique_ptr` / `std::make_unique` | `std::unique_ptr` / `std::make_unique` | [include/BS_thread_pool.hpp:323](include/BS_thread_pool.hpp#L323) [include/BS_thread_pool.hpp:358](include/BS_thread_pool.hpp#L358) [include/BS_thread_pool.hpp:2078](include/BS_thread_pool.hpp#L2078) [include/BS_thread_pool.hpp:2386](include/BS_thread_pool.hpp#L2386) | 独占所有权的智能指针，析构时 `delete` 所管对象。323 行用 `make_unique` 在堆上建类型擦除模型，避免先裸 `new` 再交接。不要对同一指针建两个 `unique_ptr`。`make_unique` 不能用于数组的自定义删除器场景。 |
| `unique_ptr` 管理数组 | `unique_ptr` to array | [include/BS_thread_pool.hpp:2078](include/BS_thread_pool.hpp#L2078) [include/BS_thread_pool.hpp:2386](include/BS_thread_pool.hpp#L2386) | `unique_ptr<T[]>` 用 `delete[]`，并用 `operator[]` 访问元素。线程数组这样分配，长度存在单独的 `thread_count` 里。它没有大小信息，也没有范围 for 所需的 `begin`；下标不能越界。 |
| `std::shared_ptr` / `std::make_shared` | `std::shared_ptr` / `std::make_shared` | [include/BS_thread_pool.hpp:678](include/BS_thread_pool.hpp#L678) [include/BS_thread_pool.hpp:2163](include/BS_thread_pool.hpp#L2163) [include/BS_thread_pool.hpp:2198](include/BS_thread_pool.hpp#L2198) [include/BS_thread_pool.hpp:2233](include/BS_thread_pool.hpp#L2233) | 共享所有权，最后一个引用离开时销毁对象。块任务把共享状态交给多个 future 或子任务。控制块是原子计数，有开销；有循环引用时要改用 `weak_ptr`。`make_shared` 把对象和控制块一次分配。 |
| `std::begin` / `std::end` | `std::begin` / `std::end` | [include/BS_thread_pool.hpp:1579](include/BS_thread_pool.hpp#L1579) | 对容器调用成员 `begin`/`end`，对数组退化为指针运算，从而同一套代码能遍历两者。1579 行用它们取得任务范围。对临时容器调用会得到悬空迭代器。优先在泛型代码里用 `std::begin`，已知是成员时直接调用也可以。 |
| `std::min` / `std::count` / `std::remove` | `std::min` / `std::count` / `std::remove` | [include/BS_thread_pool.hpp:589](include/BS_thread_pool.hpp#L589) [include/BS_thread_pool.hpp:891](include/BS_thread_pool.hpp#L891) [include/BS_thread_pool.hpp:2133](include/BS_thread_pool.hpp#L2133) [include/BS_thread_pool.hpp:2485](include/BS_thread_pool.hpp#L2485) | `min` 取较小值，`count` 数等于某值的元素，`remove` 把不等于该值的元素前移并返回新逻辑结尾，并不真正擦除。2485 行必须再配 `erase`。Windows 的 `min`/`max` 宏会破坏这里，所以头文件前面把它们 `#undef` 了。 |
| `std::sort` / `std::shuffle` | `std::sort` / `std::shuffle` | [tests/BS_thread_pool_test.cpp:804](tests/BS_thread_pool_test.cpp#L804) [tests/BS_thread_pool_test.cpp:2297](tests/BS_thread_pool_test.cpp#L2297) | `sort` 把范围排成升序，要求严格弱序且不能抛出破坏性的比较。`shuffle` 用均匀随机数引擎打乱顺序，测试用它生成无序输入。`random_shuffle` 已删除，必须传入引擎，不能只用 `rand`。 |
| erase-remove 惯用法 | erase-remove idiom | [include/BS_thread_pool.hpp:2485](include/BS_thread_pool.hpp#L2485) | `remove` 只重排不缩短容器，返回的迭代器到 `end()` 之间是移走后的尾部，再 `erase` 才真正删除。2485 行从输出流列表里去掉某个流。C++20 可写成 `std::erase` / 成员 `erase_if`，少一次手写尾部。只调用 `remove` 会留下重复的尾元素。 |
| `std::chrono::duration` / `time_point` | `std::chrono::duration` / `time_point` | [include/BS_thread_pool.hpp:498](include/BS_thread_pool.hpp#L498) [include/BS_thread_pool.hpp:535](include/BS_thread_pool.hpp#L535) [include/BS_thread_pool.hpp:537](include/BS_thread_pool.hpp#L537) [include/BS_thread_pool.hpp:556](include/BS_thread_pool.hpp#L556)；[tests/BS_thread_pool_test.cpp:204](tests/BS_thread_pool_test.cpp#L204) [tests/BS_thread_pool_test.cpp:226](tests/BS_thread_pool_test.cpp#L226) | `duration` 是“滴答数 × 周期”，`time_point` 是某时钟上的时间点。等待函数接受任意 duration，内部换算。不同周期的 duration 可以相加，结果周期取最细者。不要用整数毫秒手工换算，除非明确要截断。 |
| `steady_clock` / `system_clock` | `steady_clock` / `system_clock` | [include/BS_thread_pool.hpp:537](include/BS_thread_pool.hpp#L537)；[tests/BS_thread_pool_test.cpp:3836](tests/BS_thread_pool_test.cpp#L3836) | `steady_clock` 只向前走，适合超时和计时；`system_clock` 是挂钟时间，可被调整，适合显示。537 行的 `wait_until` 若跟随时钟，超时用 steady 更稳。两者的 `time_point` 不能直接比较。 |
| `std::ratio`（作为 duration 形参） | `std::ratio` as duration period | [include/BS_thread_pool.hpp:534-535](include/BS_thread_pool.hpp#L534)；[tests/BS_thread_pool_test.cpp:203](tests/BS_thread_pool_test.cpp#L203) | `ratio<Num, Den>` 是编译期有理数。`duration<Rep, Period>` 的 `Period` 就是它，例如 `ratio<1, 1000>` 表示毫秒。分子分母要互质时由库归一化。不要在运行期改周期；周期是类型的一部分。 |
| `time_point_cast` | `std::chrono::time_point_cast` | [tests/BS_thread_pool_test.cpp:3836](tests/BS_thread_pool_test.cpp#L3836) | 把时间点转到另一种 duration 精度，向零截断。测试在格式化前把时钟时间落到所需精度。变粗会丢精度，变细不会凭空增加信息。C++17 还有 `floor` / `ceil` / `round` 控制方向。 |
| `std::format`（C++20，含 chrono 格式） | `std::format` | [tests/BS_thread_pool_test.cpp:3836](tests/BS_thread_pool_test.cpp#L3836) | 按格式字符串生成 `string`，`{}` 是替换字段，chrono 说明符可格式化时间点。类型不匹配是编译错误（若实现做了编译期检查）或抛 `format_error`。它不写流，也不自动刷新。需要 `C++20` 且定义了 `__cpp_lib_format` 才包含 `<format>`。 |
| `std::bit_width`（C++20） | `std::bit_width` | [include/BS_thread_pool.hpp:850](include/BS_thread_pool.hpp#L850) [include/BS_thread_pool.hpp:1011](include/BS_thread_pool.hpp#L1011) | 返回表示该无符号值所需的最少位数，等于 `floor(log2(x)) + 1`，0 的结果是 0。850 行用它从亲和性掩码推出 CPU 个数。没有该特性时下面用手写右移循环。参数必须是无符号整数。 |
| `std::ostream` / `std::cout` / `std::endl` / `std::flush` | iostream | [include/BS_thread_pool.hpp:187](include/BS_thread_pool.hpp#L187) [include/BS_thread_pool.hpp:2415](include/BS_thread_pool.hpp#L2415) [include/BS_thread_pool.hpp:2491](include/BS_thread_pool.hpp#L2491) [include/BS_thread_pool.hpp:2496](include/BS_thread_pool.hpp#L2496) | `ostream` 是输出流基类，`cout` 是标准输出。`endl` 输出换行并刷新，`flush` 只刷新。2491 行把它们收成函数指针，供同步流使用。频繁 `endl` 会强制刷新，只要换行时用 `'\n'`。多个线程同时写同一个流要自己加锁，本库用 `synced_stream` 做这件事。 |
| `std::setw` | `std::setw` | [tests/BS_thread_pool_test.cpp:697](tests/BS_thread_pool_test.cpp#L697) | 操纵符，设置下一次输出字段的最小宽度，不足则填充。它只作用于紧接着的那一项，之后恢复默认。要改填充字符用 `setfill`。它不截断超长内容。 |
| `std::ofstream` 与 `std::ios::binary` | `std::ofstream` and binary open mode | [tests/BS_thread_pool_test.cpp:3368](tests/BS_thread_pool_test.cpp#L3368) [tests/BS_thread_pool_test.cpp:4041](tests/BS_thread_pool_test.cpp#L4041) | `ofstream` 把数据写入文件。`binary` 禁止实现做换行转换（Windows 上文本模式会把 `\n` 写成 `\r\n`）。打开后应检查流状态。析构会关闭文件；写入失败要看 `failbit`，不一定抛异常，除非设置了异常掩码。 |
| `std::complex` | `std::complex` | [tests/BS_thread_pool_test.cpp:3247-3251](tests/BS_thread_pool_test.cpp#L3247) [tests/BS_thread_pool_test.cpp:3348](tests/BS_thread_pool_test.cpp#L3348) | 复数，实部和虚部分别用 `real()` / `imag()`，支持算术和一些超越函数。测试用它当非平凡的任务返回值。等于比较对浮点复数不可靠。它是字面类型时可被聚合或列表初始化。 |
| `std::numeric_limits` | `std::numeric_limits` | [tests/BS_thread_pool_test.cpp:121](tests/BS_thread_pool_test.cpp#L121) | 编译期查询数值类型的最大值、最小值、是否为整数等。121 行用 `numeric_limits<ptrdiff_t>::max()` 当信号量计数上限。对 `bool`、浮点的 `min()` 含义和整数不同：浮点 `min()` 是最小正规范化值，不是最负值，最负值是 `lowest()`。 |
| `<random>`：`random_device`、`mt19937_64`、`uniform_int_distribution` | random number facilities | [tests/BS_thread_pool_test.cpp:557-559](tests/BS_thread_pool_test.cpp#L557) [tests/BS_thread_pool_test.cpp:2297](tests/BS_thread_pool_test.cpp#L2297) | `random_device` 尽量提供非确定种子，`mt19937_64` 是 64 位梅森旋转发生器，`uniform_int_distribution` 把引擎输出映射成闭区间整数。引擎要保存并复用，不要每次构造。分布对象持有状态时也不应每次数都新建（虽然均匀整数分布通常无状态）。`rand` / `srand` 质量差且不是线程安全的。 |
